/* import React from "react";
import s from "./LikeCount.module.css";

const LikeCount = props => {
let like=(eventObj)=>{
props.addLike(eventObj);
}
  return (
    <div className={s.item}>
      <div className={s.likes} onClick={like}>{props.likesCount}</div>
    </div>

  );
};

export default LikeCount; */
